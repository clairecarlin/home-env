# FIRST TIME: replace the <values>
[user]
    name = e-carlin
    email = evan@carlin.com
[core]
    excludesfile = ~/.gitignore_global
    filemode = true
[pager]
    branch = false
# If not git 2.x, you may have to comment these out
[push]
    default = simple
