
                                        ; -*-emacs-lisp-*-

;; Added by Package.el.  This must come before configurations of
;; installed packages.  Don't delete this line.  If you don't want it,
;; just comment it out by adding a semicolon to the start of the line.
;; You may delete these explanatory comments.
(package-initialize)

(let
    ;; Reversed, because add-to-list inserts in front of list
  ((dirs '("~/src/e-carlin/home-env/emacs.d" "~/emacs" "~/.emacs.d/lisp")))
  (dolist (p dirs)
    (add-to-list 'load-path p)))
(ignore-errors
    (require 'pre-bivio-init))
(require 'bivio-init)
(require 'e-carlin-init)
(ignore-errors
  (require 'post-bivio-init))
(custom-set-variables
 ;; custom-set-variables was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 '(package-selected-packages
   '(company flymake-json flycheck srefactor slime lsp-ui arduino-mode xterm-color pyenv-mode xclip find-file-in-repository evil-commentary helm-lsp lsp-python-ms lsp-mode))
 '(safe-local-variable-values '((eval sh-set-shell "rpm" nil nil))))
(custom-set-faces
 ;; custom-set-faces was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 )
